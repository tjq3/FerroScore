% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_ferroptosis_analysis.R
\name{run_ferroptosis_analysis}
\alias{run_ferroptosis_analysis}
\title{Run Ferroptosis Analysis for Multiple Samples}
\usage{
run_ferroptosis_analysis(
  counts_data,
  meta_data,
  control_group = "control",
  species = "mouse",
  ppi_file = NULL,
  output_dir = tempdir(),
  save_intermediate = FALSE,
  parallel = FALSE,
  n_cores = 2
)
}
\arguments{
\item{counts_data}{A matrix or data frame of raw counts with genes as rows and samples as columns.}

\item{meta_data}{Data frame containing sample metadata (must contain 'sample_id' and 'group' columns).}

\item{control_group}{Name of the control group in meta_data (default = "control").}

\item{species}{Character string specifying the species ("mouse" or "human", default = "mouse").}

\item{ppi_file}{Path to the PPI network file (STRING database format). If NULL, uses built-in data.}

\item{output_dir}{Directory to save results (default = tempdir()).}

\item{save_intermediate}{Logical indicating whether to save intermediate results (default = FALSE).}

\item{parallel}{Logical indicating whether to use parallel processing (default = FALSE).}

\item{n_cores}{Number of cores for parallel processing (default = 2).}
}
\value{
A list containing:
\itemize{
  \item results: List of detailed analysis results for each sample
  \item summary: Data frame of ferroptosis scores for all samples
  \item merged_DEG: Combined differential expression results
}
}
\description{
Executes the complete ferroptosis analysis pipeline for multiple samples, including:
- Differential expression analysis
- PPI network construction
- Functional module detection
- Ferroptosis score calculation
}
\examples{
\dontrun{
# Example usage:
results <- run_ferroptosis_analysis(
  counts_data = counts_matrix,
  meta_data = sample_metadata,
  control_group = "control",
  species = "mouse"
)
}
}
